# ─────────────────────────────────────────────
# MyApartment Intercom — Environment Vars
# Copy this to .env.local for local dev
# Add these same vars in Vercel Dashboard → Settings → Environment Variables
# ─────────────────────────────────────────────

# Supabase — get from: https://app.supabase.com → Project Settings → API
VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
VITE_SUPABASE_ANON_KEY=your_anon_key_here

# VAPID Keys for Web Push Notifications
# Generate once with: npx web-push generate-vapid-keys
# Public key goes here AND in Supabase Edge Function env vars
VITE_VAPID_PUBLIC_KEY=your_vapid_public_key_here

# Society Config
VITE_SOCIETY_NAME=MyApartment
VITE_ADMIN_EMAIL=admin@myapartment.com
VITE_GUARD_EMAIL=guard@myapartment.com

# NEVER commit .env.local to GitHub — it is in .gitignore
